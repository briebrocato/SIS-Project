import java.util.Arrays;

public class SortStudents
	{
		 public static void sortRoster()
		        {
		        	
		        for(int i = 0; i < Student.roster.size(); i++)
		            {
		            
		            Student.roster.get(i).getLastName();   
		            char lnArray[] = lastName.toCharArray();
		            
		            Student.roster.get(i).getGPA();   
		            char gpaArray[] = GPA.toCharArray();
		            
		            Student.roster.get(i).getPeriod();   
		            char pArray[] = period.toCharArray();
		            }
		        
		        Arrays.sort(lnArray);
		        Arrays.sort(gpaArray);
		        Arrays.sort(pArray);
		        
		        
		            
		        }
		 
		 
	}
